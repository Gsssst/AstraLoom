## Implementation

- [x] Update `backend/Dockerfile` to support configurable apt mirror rewriting.
- [x] Add apt retry and timeout options to package metadata and install commands.
- [x] Add configurable pip index, retry, and timeout options to Python dependency installation.
- [x] Validate the OpenSpec change and Dockerfile syntax.
- [x] Run or document the backend Docker build result.
