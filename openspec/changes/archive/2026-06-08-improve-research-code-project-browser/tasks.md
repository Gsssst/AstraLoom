## 1. Existing Flow Mapping

- [x] 1.1 Locate the research Idea code generation UI, project package types, and download handlers.
- [x] 1.2 Identify the safest component boundary for a reusable project browser.

## 2. Project Browser Implementation

- [x] 2.1 Add a generated project browser with metadata, commands, entrypoints, safety notes, and download/regenerate actions.
- [x] 2.2 Add a folder-aware file tree with stable selection defaults and responsive wrapping.
- [x] 2.3 Add a read-only file preview with path, language, purpose, line count, and copy action.
- [x] 2.4 Preserve legacy generated-code fallback for Ideas without structured packages.

## 3. Verification

- [x] 3.1 Add frontend contract tests for file-tree, file-preview, command-copy, and legacy fallback expectations.
- [x] 3.2 Run OpenSpec validation, frontend contract tests, production build, and browser verification.
