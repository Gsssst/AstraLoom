## 1. Header Account Control

- [x] 1.1 Replace the generic account wrapper with a dedicated header account chip container
- [x] 1.2 Add alignment, line-height, truncation, hover, and mobile styles for the account chip

## 2. Verification

- [x] 2.1 Run the frontend production build (`vite build` passes; the full TypeScript build remains blocked by pre-existing writing-module errors)
- [x] 2.2 Validate the OpenSpec change in strict mode
